{
  "name": "audiobook-app",
  "version": "1.0.0",
  "description": "This is a separate project for the mobile audiobook app.",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "setup": "npm run prepare-www && npm run add-platform",
    "prepare-www": "node -e \"require('fs').mkdirSync('www', {recursive:true})\" && node -e \"['index.html','style.css','app.js','manifest.json','sw.js','mudrosti-db.js','desktop-features.js'].forEach(f=>require('fs').copyFileSync(f,'www/'+f))\" && node -e \"require('fs').cpSync('assets','www/assets',{recursive:true})\"",
    "add-platform": "npx cap add android 2>/dev/null || (echo 'Android platform already exists or failed to add' && exit 0) && npx cap sync",
    "sync": "npx cap sync",
    "copy": "npx cap copy",
    "build:android": "npx cap sync && npx cap open android",
    "update": "npx cap update"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "@capacitor/android": "^8.1.0",
    "@capacitor/cli": "^8.1.0",
    "@capacitor/core": "^8.1.0"
  }
}
